# API base URL of the NestJS backend
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Supabase (used for auth in real mode)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Demo mode: set to "true" to run the UI on in-memory mock data (no backend needed)
# NEXT_PUBLIC_USE_MOCK_DATA=true
